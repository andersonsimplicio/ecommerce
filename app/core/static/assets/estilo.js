
try {
   id_username= document.querySelector('#id_username');
   id_username.className += "form-control";
 }
 catch (e) {
    // declarações para manipular quaisquer exceções
    console.log(e); // passa o objeto de exceção para o manipulador de erro
 }
 try {
   id_password= document.querySelector('#id_password');
   id_password.className += "form-control";
 }
 catch (e) {
    // declarações para manipular quaisquer exceções
    console.log(e); // passa o objeto de exceção para o manipulador de erro
 }
 try {
   id_password1= document.querySelector('#id_password1');
   id_password1.className += "form-control";
 }
 catch (e) {
    // declarações para manipular quaisquer exceções
    console.log(e); // passa o objeto de exceção para o manipulador de erro
 }
 try {
   id_password2= document.querySelector('#id_password2');
   id_password2.className += "form-control";
 }
 catch (e) {
    // declarações para manipular quaisquer exceções
    console.log(e); // passa o objeto de exceção para o manipulador de erro
 }

 try {
   id_name= document.querySelector('#id_name');
   id_name.className += "form-control";
 }
 catch (e) {
    // declarações para manipular quaisquer exceções
    console.log(e); // passa o objeto de exceção para o manipulador de erro
 }

  try {
   id_email= document.querySelector('#id_email');
   id_email.className += "form-control";
 }
 catch (e) {
    // declarações para manipular quaisquer exceções
    console.log(e); // passa o objeto de exceção para o manipulador de erro
 }
